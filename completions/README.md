Bash / Zsh completions for torrctl.

Oh my Zsh installation:

```sh
mkdir -p ~/.oh-my-zsh/custom/plugins/torrctl
cp completions/* ~/.oh-my-zsh/custom/plugins/torrctl # from project root
```

then add "torrctl" to "plugins" array.
